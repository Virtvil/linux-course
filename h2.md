# Harjoitus 2: Voileipä

## Materiaalit

Sudo without password
- Ansiblea varten tarvitaan jokaiselle "orjakoneelle" oma root-hakemisto
- Näillä muutoksia tehtäessä olisi hyvä ettei joka ikinen kerta muutoksia tehdessä kysytä sudo-salasanaa
- Tätä varten: luo käyttäjä, uusi sudoless ryhmä ja lisää käyttäjä tänne
  (_sudo adduser kayttajanimi, sudo groupadd sudoless, sudo adduser kayttajanimi sudoless_)
- Varmista ettei sudo hajoa (tämän hajotessa voi lentää kirosana tai useampi) avaamalla uusi ikkuna, yhdistämällä ssh yhteydellä koneeseen ja pitämällä root shell auki (komento: _sudo -i_)
- Uudella säännöllä käyttäjät saadaan luotuun sudoless ryhmään jossa näiltä ei kysytä sudo salasanaa. (Käytä komentoa: _sudo visudo /etc/sudoers.d/sudoless_ ja lisää rivi _%sudoless ALL = (ALL) NOPASSWD: ALL_)
- Varmista toimivuus (komento: _ssh kayttajanimi@localhost_, _sudo -k_)

<img width="360" height="299" alt="sandwich" src="https://github.com/user-attachments/assets/d020b77e-dd9d-4d9c-a4f4-044b015aec61" />

Passwordless Sudo with Ansible
- Aiemmat salasanattomat käytännöt saadaan myös luotua Ansiblella
- 



